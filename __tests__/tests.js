const request = require('supertest');
const app = require('../server');
const db = require('../schemas');

describe('testing the response of the API', () => {});

describe('adding new user auth', () => {});

describe('running the auth process', () => {});
